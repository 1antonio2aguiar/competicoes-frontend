import { HttpParams } from '@angular/common/http';

export class Filters {
    pagina = 0;
    itensPorPagina = 10;
    totalRegistros = 0;
    params = new HttpParams();

    campeonatoId = 0; 
    campeonato = 0; 
  
}
